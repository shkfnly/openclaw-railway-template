services:
  openclaw:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - SETUP_PASSWORD=test
      - OPENCLAW_STATE_DIR=/data/.openclaw
      - OPENCLAW_WORKSPACE_DIR=/data/workspace
      - ENABLE_WEB_TUI=true
    volumes:
      - ./.tmpdata:/data
